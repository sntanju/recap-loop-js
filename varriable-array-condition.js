// variable
var bottleColor = 'yellow';
var waterQuantity = 1;
var isFull = false;

// array
var items = ['bottle', 'mug', 'paper', 'pen'];
items.indexOf('logens'); // -1
items.push('evelope');
items.push('watch');
items.pop();

// conditionals
if(items.length >= 4){
console.log('yu have to many');
}
else if (items.length == 4){
    console.log('four eelement');
}
else{
    console.log('wowwoe');
}
